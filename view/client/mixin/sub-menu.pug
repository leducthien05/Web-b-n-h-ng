mixin submenu(category, path)
    ul
        each item in category
            li
                a(href= path + '/' + item.slug)= item.title
                if( item.children && item.children.length > 0)
                    +submenu(item.children, path)
